rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read for menu photos
      allow read: if true;
      // Allow authenticated users to upload their restaurant assets
      allow write: if request.auth != null;
    }
  }
}
